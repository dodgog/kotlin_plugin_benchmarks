name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  kotlin_plugin:
    path: ../
  
  jni: ^0.14.1
  path_provider: ^2.1.5
  path: ^1.9.1
  drift: ^2.26.0
  drift_flutter: ^0.2.4
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2
  drift_dev: ^2.26.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true

